import { FormCouponProps } from "@/app/(panneladmin)/(admin)/admin/coupons/types/formCouponType"

const FormCoupon = ({formData,type,changeHandler,formHandler,isLoading,expireDate,onChangeSelect,setExpireDate,setType,options}:FormCouponProps) => {
  return (
    <div>FormCoupon</div>
  )
}
export default FormCoupon